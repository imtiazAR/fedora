#!/usr/bin/env bash

set -euo pipefail

if (( EUID != 0 )); then
    SUDO="sudo"
else
    SUDO=""
fi

echo "=================================="
echo " Fedora Post Install"
echo "=================================="

# --------------------------------------------------
# Configure DNF
# --------------------------------------------------

echo "[1/6] Configuring DNF..."

$SUDO tee /etc/dnf/dnf.conf >/dev/null <<'EOF'
[main]
max_parallel_downloads=10
fastestmirror=true
defaultyes=true
keepcache=true
#excludepkgs=libreoffice*,firefox*
EOF

# --------------------------------------------------
# Enable RPM Fusion
# --------------------------------------------------

echo "[2/6] Enabling RPM Fusion..."

FEDORA_VERSION="$(rpm -E %fedora)"

$SUDO dnf install -y \
"https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-${FEDORA_VERSION}.noarch.rpm" \
"https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-${FEDORA_VERSION}.noarch.rpm"

# --------------------------------------------------
# Enable AppStream metadata
# --------------------------------------------------

echo "[3/6] Updating AppStream..."

$SUDO dnf group upgrade -y core

# --------------------------------------------------
# Multimedia
# --------------------------------------------------

echo "[4/6] Installing multimedia codecs..."

$SUDO dnf swap -y ffmpeg-free ffmpeg --allowerasing

$SUDO dnf group upgrade -y multimedia 


# --------------------------------------------------
# Firmware
# --------------------------------------------------

echo "[5/6] Refreshing firmware metadata..."

fwupdmgr refresh --force || true
fwupdmgr get-updates

# --------------------------------------------------
# System Update
# --------------------------------------------------

echo "[6/6] Updating system..."

$SUDO dnf upgrade --refresh -y

echo
echo "=================================="
echo " Fedora setup completed!"
echo " Please reboot your system."
echo "=================================="
